# Optional: copy to .env.local and fill in.
# Supabase (required for auth and Edge Functions)
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key

# Stripe: use pk_test_... for test mode so Stripe recognizes the key (avoids wallet-config 401 for Apple/Google Pay).
# Get keys from https://dashboard.stripe.com (Developers → API keys).
#VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...
